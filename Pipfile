[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
requests = "*"

[dev-packages]
pytest = "*"
responses = "*"
pytest-socket = "*"
pytest-mock = "*"

[requires]
python_version = "3.10"

[scripts]
test = "pytest --disable-socket"
